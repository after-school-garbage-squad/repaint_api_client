name: repaint_api_client
version: 1.0.0
description: OpenAPI API client
homepage: https://repaint.asgs.dev/
repository: https://github.com/after-school-garbage-squad/repaint_api_client.git

environment:
  sdk: '>=2.15.0 <3.0.0'

dependencies:
  dio: '^5.2.0'
  json_annotation: '^4.4.0'

dev_dependencies:
  build_runner: any
  json_serializable: '^6.1.5'
  test: ^1.16.0
